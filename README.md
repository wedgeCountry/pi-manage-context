# pi-manage-context
A pi extension to manage and modify context during a session

Type /manage-context in the pi session. 
You will be able to select/deselect messages and tool results. Only selected messages will be used in the llms context.

There is also the possibility to delete and to compress messages from the context. In both cases you will be asked for confirmation.

There is also a preview window to see the context content.
